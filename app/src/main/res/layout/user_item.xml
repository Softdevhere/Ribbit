<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@android:color/white"
    >

    <ImageView
        android:id="@+id/friendImageView"
        android:src="@drawable/avatar_empty"
        android:contentDescription="@string/no_avatar_image_label"
        style="@style/grid_image_style"/>

    <ImageView
        android:id="@+id/checkMarkImageView"
        android:src="@drawable/avatar_selected"
        android:contentDescription="@string/user_grid_checkmark_label"
        style="@style/grid_image_style"
        android:visibility="invisible"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="44dp"
        android:text="New Text"
        android:id="@+id/nameLabel"
        android:layout_below="@id/friendImageView"
        android:layout_centerHorizontal="true"
        android:padding="4dp"
        android:maxLines="2"
        />


</RelativeLayout>